# I am struggling with getting the list to work
- hosts: localhost
  tasks:
  - name: All the libraries i love
    become: true
    ansible.builtin.apt:
      pkg:
      - fzf
      - ripgrep
      - pavucontrol
      - xclip
      - jq
      - tldr
      - shutter
